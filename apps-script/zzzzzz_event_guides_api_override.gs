/**
 * Deprecated compatibility file.
 *
 * The public API router and final payload builders live only in
 * zzzzzzzz_portal_final_api_override.gs. This file intentionally defines no
 * functions so Apps Script cannot let an older event-guide endpoint override
 * the current master-data flow.
 */
